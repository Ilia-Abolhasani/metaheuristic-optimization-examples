

function z=MyCost(x)

n = numel (x);

   z1= 3.* x(1) + 4.* x(2) + 4.* x(3) + 4.* x(4); 
   st :
    0.9 .* x(3) + 1.2 .* x(2)  < 4 .* ( x(3) + x(2)) ;
    
    z2=1-exp(-sum((x-1/sqrt(n)).^2)); 

    
    
    z =[z1
        z2];

end